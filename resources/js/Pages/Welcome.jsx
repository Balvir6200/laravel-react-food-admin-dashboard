import React from 'react';

export default function Welcome() {
    return (
        <div style={{
            minHeight: '100vh',
            display: 'flex',
            alignItems: 'center',
            justifyContent: 'center',
            fontSize: '32px',
            fontWeight: '700'
        }}>
            React + Laravel + Inertia is working successfully
        </div>
    );
}